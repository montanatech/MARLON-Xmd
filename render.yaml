services:
  - type: web
    name: POPKID-AI
    env: node
    plan: free
    repo: https://github.com/popkidc/POPKID-XD
    branch: main
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: SESSION_ID
        sync: false  # Don't include the value here, will be entered manually on Render
      - key: OWNER_NUMBER
        sync: false
